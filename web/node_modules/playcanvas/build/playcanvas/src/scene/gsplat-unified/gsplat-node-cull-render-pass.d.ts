/**
 * @import { GraphicsDevice } from '../../platform/graphics/graphics-device.js'
 * @import { Texture } from '../../platform/graphics/texture.js'
 */
/**
 * Render pass for GPU frustum culling of bounding spheres. Reads local-space spheres and
 * transform indices, reconstructs world matrices, and tests against camera frustum planes.
 * Outputs a bit-packed R32U visibility texture (each texel holds 32 sphere results as bits).
 *
 * @ignore
 */
export class GSplatNodeCullRenderPass extends RenderPassShaderQuad {
    /** @type {Texture} */
    _boundsSphereTexture: Texture;
    /** @type {Texture} */
    _boundsTransformIndexTexture: Texture;
    /** @type {Texture} */
    _transformsTexture: Texture;
    /** @type {number} */
    _totalBoundsEntries: number;
    /** @type {Float32Array} */
    _frustumPlanes: Float32Array;
    boundsSphereTextureId: import("../../index.js").ScopeId;
    boundsTransformIndexTextureId: import("../../index.js").ScopeId;
    transformsTextureId: import("../../index.js").ScopeId;
    boundsTextureWidthId: import("../../index.js").ScopeId;
    transformsTextureWidthId: import("../../index.js").ScopeId;
    totalBoundsEntriesId: import("../../index.js").ScopeId;
    frustumPlanesId: import("../../index.js").ScopeId;
    /**
     * @param {Texture} boundsSphereTexture - The bounds sphere texture.
     * @param {Texture} boundsTransformIndexTexture - The transform index texture.
     * @param {Texture} transformsTexture - The transforms texture.
     * @param {number} totalBoundsEntries - Total number of bounds entries.
     * @param {Float32Array} frustumPlanes - 24 floats: 6 planes x (nx, ny, nz, distance).
     */
    setup(boundsSphereTexture: Texture, boundsTransformIndexTexture: Texture, transformsTexture: Texture, totalBoundsEntries: number, frustumPlanes: Float32Array): void;
}
import { RenderPassShaderQuad } from '../graphics/render-pass-shader-quad.js';
import type { Texture } from '../../platform/graphics/texture.js';
